# ==============================================================================
# HAOS Linux - ZRAM Configuration
# Compactação transparente de memória RAM via zstd
# ==============================================================================
[zram0]
zram-size = ram / 2
compression-algorithm = zstd
swap-priority = 100
fs-type = swap
